package main

import "fmt"

func main() {
	fmt.Println("hello world")

	// parse arguments: profile, congig, env (set, load, sync flags env, application)
	// switch per command
	// profile - name
	// config app - name flags dir?
	// env create - name
	// env delete - name
	//  set - name, value
	// load path env?
	// sync dir? env? file?

}
